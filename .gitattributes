# 줄바꿈을 플랫폼에 맞게 자동 정규화 (저장소에는 LF 로 보관)
* text=auto

# 셸 스크립트는 어디서 받든 반드시 LF 여야 한다.
# CRLF 가 섞이면 macOS/Linux 에서 "bad interpreter: /bin/bash^M" 오류가 난다.
*.sh text eol=lf

# 배치 파일은 반대로 CRLF 가 안전하다.
*.bat text eol=crlf

# CSV 는 MATLAB·엑셀이 둘 다 읽으므로 변환하지 않는다.
*.csv -text
